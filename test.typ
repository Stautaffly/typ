
#let x = ("8":"9")  //前面是键 后面是对 调用函数时
#x.at("8")\ //"键"
#set text(size: 17pt)
```typ
#x.insert("8","7") //插入不同的对时会更新对值
#x.insert("1","2")//只能一键值对形式插入
#x.at("8")\
#x.at("2",default:"hh")\
#x.keys() \ // 返回所有的键值
#x.values()// 返回所有的对值
111
```
#rect([#x])




#x.insert("8","7") //插入不同的对时会更新对值
#x.insert("1","2")//只能一键值对形式插入
#x.at("8")\
#x.at("2",default:"hh")\
#x.keys() \ // 返回所有的键值
#x.values()// 返回所有的对值

#let A = (1,2)

313131


